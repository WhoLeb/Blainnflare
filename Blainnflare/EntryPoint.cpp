#include "src/Core/Window.h"

int WINAPI WinMain(HINSTANCE hInstance, HINSTANCE hPrevInstance,
		PSTR pCmdLine, int nCmdShow)
{
	Blainn::Window* window = Blainn::Window::Create(hInstance);
	while (true)
	{

	}
}
